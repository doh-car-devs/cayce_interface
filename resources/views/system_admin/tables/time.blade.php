
<div id="mydraggable" class="container mb-5"></div>
<div id="calendar" class="container mb-5"></div>

{{-- <table id="timetable" class="table table-bordered table-sm table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Time</th>
            <th>Type</th>
        </tr>
    </thead>
    <tbody class="wfptablebody">
        @if ($data['time'])
            @foreach ($data['time'] as $user)
                <tr>
                    <td>
                        {{$user['checktime']}}
                    </td>
                    <td>
                        {{$user['checktype']}}
                    </td>
                </tr>
            @endforeach
        @endif
    </tbody>
</table> --}}
